# Test + dev tooling (kept separate from the runtime requirements.txt).
# Install:  pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0
pytest-cov>=5.0
pytest-mock>=3.14
freezegun>=1.5
