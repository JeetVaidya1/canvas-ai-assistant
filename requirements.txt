# Web framework
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
requests>=2.32

# LLM + vision (Claude) and local embeddings
anthropic>=0.40
sentence-transformers>=3.0
faiss-cpu>=1.8.0
tiktoken>=0.6.0

# Exports (Notes PDF, Anki deck; iCal is hand-rolled)
markdown>=3.6
xhtml2pdf>=0.2.17
genanki>=0.13

# Database / storage
supabase>=2.0
python-dotenv>=1.0

# Document ingestion
pdfplumber>=0.10.2
PyMuPDF>=1.24
python-pptx>=0.6.23
python-docx>=1.1.0
pdf2image>=1.17          # requires system 'poppler' (brew install poppler)
pytesseract>=0.3.11      # requires system 'tesseract' (brew install tesseract)
Pillow>=10.3.0
opencv-python>=4.9
numpy>=1.26
