[pytest]
# App modules live at the repo root; put it on the path so `import deps` etc. work.
pythonpath = .
testpaths = tests
addopts = -ra
markers =
    unit: fast, isolated, no network/DB
    smoke: import-only safety checks for refactor protection
